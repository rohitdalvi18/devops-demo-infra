name: UAT Pipeline

on:
#   workflow_run:
    # workflows: ["QA Pipeline"]
    # types: [completed]
  # allow manual re-runs if needed
  repository_dispatch:
    types: [UAT Pipeline]

  workflow_dispatch:
    inputs:
        image_tag:
          description: 'ECR Image Tag (e.g., nightly-42)'
          required: true

# Grant rights to create & comment on issues
permissions:
    contents: read
    issues: write

env:
  AWS_REGION:     us-east-1
  AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
  AWS_DEFAULT_REGION: us-east-1
  ECR_REGISTRY:   ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.us-east-1.amazonaws.com
  CLUSTER_NAME:   devops-demo
  IMAGE_TAG:      ${{ github.event.client_payload.image_tag || github.event.inputs.image_tag }}

jobs:
  verify-image-tag:
    runs-on: ubuntu-latest
    steps:
      - name: Check image tag
        run: | 
            echo "Deploying image: $IMAGE_TAG"

  await-approval:
    # only run when QA succeeded
    needs: verify-image-tag
    # sws if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Pause for manual approval 🛑
        uses: trstringer/manual-approval@v1
        with:
          secret: ${{ secrets.GH_token }}
          approvers: rgdalvi            # GitHub username of the approver
          minimum-approvals: 1
          issue-title: "Approve UAT Deployment"
          issue-body: |
            QA pipeline has passed and awaits promotion to UAT

            Deploying image `${IMAGE_TAG}` to UAT.

            • You can view QA pipeline run details:  
              ${{ github.event.workflow_run.html_url }}

            🟢 Please comment "approved" or "lgtm" or "yes" to continue!  
            
            🔴 If you wish to cancel, comment “deny” or "no"

  deploy-uat:
    needs: await-approval
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token:     ${{ secrets.AWS_SESSION_TOKEN }}  
          aws-region:            us-east-1

      - name: Setup kubectl
        run: |
          KUBECTL_VERSION=$(curl -sL https://dl.k8s.io/release/stable.txt)
          echo "Fetched KUBECTL_VERSION=$KUBECTL_VERSION"
          curl -LO https://dl.k8s.io/release/${KUBECTL_VERSION}/bin/linux/amd64/kubectl
          chmod +x kubectl
          sudo mv kubectl /usr/local/bin/
          kubectl version --client

      - name: Update kubeconfig
        run: aws eks update-kubeconfig --region us-east-1 --name ${CLUSTER_NAME}

      - name: Patch images in base manifests
        run: |
          sed -i "s|REPLACE_LOGIN_IMAGE|${ECR_REGISTRY}/login-service:${IMAGE_TAG}|g" k8s/base/login-deploy.yaml
          sed -i "s|REPLACE_ORDER_IMAGE|${ECR_REGISTRY}/order-service:${IMAGE_TAG}|g" k8s/base/order-deploy.yaml
          sed -i "s|REPLACE_INVENTORY_IMAGE|${ECR_REGISTRY}/inventory-service:${IMAGE_TAG}|g" k8s/base/inventory-deploy.yaml
          sed -i "s|REPLACE_FRONTEND_IMAGE|${ECR_REGISTRY}/frontend-service:${IMAGE_TAG}|g" k8s/base/frontend-deploy.yaml

      - name: Create namespace
        run: |
          kubectl create namespace uat || echo "Namespace already exists"

      - name: Deploy to UAT namespace
        run: kubectl apply -k k8s/overlays/uat

      - name: Wait for UAT rollouts
        run: |
          for dep in login order inventory frontend; do
            kubectl rollout status deploy/$dep -n uat --timeout=120s
          done

      - name: UAT TEST - AUTH Endpoint verification
        run: |
          HOST=$(kubectl get svc login -n uat -o jsonpath='{.status.loadBalancer.ingress[0].hostname}')
          echo "UAT login is at http://$HOST"

      - name: UAT TEST - Test frontend microservice
        run: |
          HOST=$(kubectl get svc frontend -n uat -o jsonpath='{.status.loadBalancer.ingress[0].hostname}')
          echo "UAT frontend is at http://$HOST"

      - name: UAT TEST - Order List Render Check
        run: |
          HOST=$(kubectl get svc order -n uat -o jsonpath='{.status.loadBalancer.ingress[0].hostname}')
          echo "UAT order is at http://$HOST"
      
      - name: UAT TEST - Inventory Visibility Test
        run: |
          HOST=$(kubectl get svc inventory -n uat -o jsonpath='{.status.loadBalancer.ingress[0].hostname}')
          echo "UAT inventory is at http://$HOST"

      - name: Cleanup UAT namespace
        run: |
            kubectl delete namespace uat                    
            kubectl wait --for=delete namespace uat --timeout=200s

